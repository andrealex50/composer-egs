```mermaid
graph TB
    subgraph Frontend
        USER["👤 Fãs / Compradores<br/>Frontend React"]
    end

    USER -->|Pedidos| COMPOSER

    subgraph GATEWAY["Composer / API Gateway — FastAPI :8080"]
        COMPOSER["<b>Composer / API Gateway</b><br/>FastAPI"]
        
        AUTH_ROUTES["<b>Auth</b><br/>POST /api/auth/register<br/>POST /api/auth/login<br/>POST /api/auth/refresh<br/>GET /api/auth/me<br/>POST /api/auth/logout<br/>POST /api/auth/forgot-password<br/>POST /api/auth/reset-password<br/>DELETE /api/auth/me"]
        EVENT_ROUTES["<b>Events</b><br/>GET|POST /api/events<br/>GET|PUT|DEL /api/events/{id}<br/>POST /api/events/{id}/tickets<br/>GET /api/events/{id}/tickets<br/>GET /api/tickets/{id}/availability"]
        RES_ROUTES["<b>Reservations</b><br/>POST /api/reservations<br/>GET /api/reservations/{id}"]
        PAY_ROUTES["<b>Payments</b><br/>GET|POST /api/payments<br/>GET /api/payments/{id}<br/>POST /api/payments/{id}/confirm<br/>POST /api/payments/{id}/cancel<br/>GET /api/payments/{id}/receipt"]
        ORCH_ROUTES["<b>Orquestrações Saga</b><br/>POST /api/checkout<br/>GET /api/checkout/success<br/>GET /api/checkout/cancel<br/>POST /api/refund<br/>GET /health"]
    end

    subgraph SERVICES["Serviços de Negócio Independentes"]
        subgraph AUTH_BOX["Auth Service"]
            AUTH["Auth API<br/>Gerenciamento de identidades, JWT e password reset"]
        end

        subgraph INV_BOX["Inventory Service"]
            INV["Inventory API<br/>Gestão de eventos, bilhetes e reservas"]
        end

        subgraph PAY_BOX["Payment Service"]
            PAY["Payment API<br/>Processamento de pagamentos pela Stripe"]
        end
    end

    COMPOSER --> AUTH_ROUTES
    COMPOSER --> EVENT_ROUTES
    COMPOSER --> RES_ROUTES
    COMPOSER --> PAY_ROUTES
    COMPOSER --> ORCH_ROUTES

    AUTH_ROUTES -->|"proxy"| AUTH
    EVENT_ROUTES -->|"proxy"| INV
    RES_ROUTES -->|"proxy"| INV
    PAY_ROUTES -->|"proxy"| PAY
    ORCH_ROUTES -->|"Auth + Inventory + Payment"| AUTH
    ORCH_ROUTES -->|"saga"| INV
    ORCH_ROUTES -->|"saga"| PAY

    style GATEWAY fill:#2d2d4e,stroke:#6366f1,color:#fff
    style AUTH_BOX fill:#1e1e3a,stroke:#8b5cf6,color:#fff
    style INV_BOX fill:#1e1e3a,stroke:#8b5cf6,color:#fff
    style PAY_BOX fill:#1e1e3a,stroke:#8b5cf6,color:#fff
    style Frontend fill:#1e3a5f,stroke:#3b82f6,color:#fff
    style SERVICES fill:#0d0d1a,stroke:#6366f1,color:#fff
```
